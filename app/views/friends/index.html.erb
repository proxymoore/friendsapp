<%= notice %></p>

<h1>Friend List</h1>

<div id="friends">

<table class="table table-stripped tabled-bordered table-hover">
  <thead class="thead-dark">
    <tr>
    <th>First Name</th>
    <th>Last Name</th>
    <th>Email</th>
    <th>Phone</th>
    <th>Twitter</th>
    <th>Show Friend</th>
    <th>Delete</th>

  <% @friends.each do |friend| %>
  <%= render friend %>

  <% end %>
  <br><br>
  Want to add someone to your list?<br>
  <%= link_to "Add a New friend", new_friend_path, class:"btn btn-secondary" %>
</div>
</table>

